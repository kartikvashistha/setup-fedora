---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: setup-fedora
    image: docker.io/fedora:latest
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
lint: |
  yamllint .
